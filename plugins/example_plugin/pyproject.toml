[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pylium-example-plugin"
version = "0.1.0"
dependencies = [
    "requests>=2.31.0",  # Example dependency
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["pylium.*"] 